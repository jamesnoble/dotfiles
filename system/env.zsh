# Only set this if we haven't set $EDITOR up somewhere else previously.
if [[ "$EDITOR" == "" ]] ; then
  # Use sublime for my editor.
  export EDITOR=/usr/local/Cellar/macvim/7.4-73_1/MacVim.app/Contents/MacOS/Vim
fi
